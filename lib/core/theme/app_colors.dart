import 'package:flutter/material.dart';

class AppColors {
  static Color? primaryColor = Colors.blueAccent;
  static Color? whiteColor = Colors.white;
  static Color? blackColor = Colors.black;
  static Color? black12 = Colors.black12;
  static Color? black26 = Colors.black26;
  static Color? black38 = Colors.black38;
  static Color? black45 = Colors.black45;
  static Color? grey7Color = Colors.grey[700];
  static Color? grey6Color = Colors.grey[600];
  static Color? grey5Color = Colors.grey[500];
  static Color? grey4Color = Colors.grey[400];
  static Color? grey3Color = Colors.grey[300];
  static Color? grey2Color = Colors.grey[200];
  // static Color? primaryColor = Colors.grey[350];

  // ── Primary Brand ─────────────────────────────────────────────────────────
  static const Color primary = Color(0xFF1A73E8); 
  static const Color primaryDark = Color(0xFF0D47A1); 
  static const Color accent = Color(0xFFFF6B35); 

  // ── Ink / Text ────────────────────────────────────────────────────────────
  static const Color ink900 = Color(0xFF0A0E1A);
  static const Color ink700 = Color(0xFF1C2340);
  static const Color ink500 = Color(0xFF4A5272);
  static const Color ink300 = Color(0xFF8E97B0);
  static const Color ink100 = Color(0xFFE8EAF0);

  // ── Surface ───────────────────────────────────────────────────────────────
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceWarm = Color(0xFFF8F9FC);
  static const Color surfaceCard = Color(0xFFF2F4F8);
  static const Color surfaceDark = Color(0xFF0F1221);

  // ── Category chip colors ──────────────────────────────────────────────────
  static const Color catBusiness = Color(0xFF1A73E8);
  static const Color catTech = Color(0xFF9333EA);
  static const Color catHealth = Color(0xFF16A34A);
  static const Color catSports = Color(0xFFEA4335);
  static const Color catEntertainment = Color(0xFFFF6B35);
  static const Color catScience = Color(0xFF0891B2);

  // ── Utility ───────────────────────────────────────────────────────────────
  static const Color divider = Color(0xFFE8EAF0);
  static const Color shimmer = Color(0xFFEEF0F5);
  static const Color shimmerHighlight = Color(0xFFFAFBFC);
  static const Color saved = Color(0xFFFFC107);
  static const Color error = Color(0xFFDC2626);
  static const Color success = Color(0xFF16A34A);

  // ── Glass overlay ─────────────────────────────────────────────────────────
  static const Color glassWhite = Color(0x33FFFFFF); 
  static const Color glassDark = Color(0x66000000); 
}
